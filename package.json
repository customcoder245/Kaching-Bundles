{
  "name": "kaching_bundles",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "concurrently -n FRONTEND,BACKEND,TUNNEL -c cyan,green,yellow \"npm run dev --workspace=frontend\" \"npm run dev --workspace=backend\" \"npm run tunnel\"",
    "dev:frontend": "npm run dev --workspace=frontend",
    "dev:backend": "npm run dev --workspace=backend",
    "dev:tunnel": "concurrently -n FRONTEND,BACKEND,TUNNEL -c cyan,green,yellow \"npm run dev --workspace=frontend\" \"npm run dev --workspace=backend\" \"npm run tunnel\"",
    "tunnel": "cloudflared tunnel --url http://localhost:5000",
    "lint": "npm run lint --workspace=frontend"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}